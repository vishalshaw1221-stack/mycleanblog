<footer>
  <div class="footer-inner">

    <!-- SOCIAL ICONS (LOCKED ZONE) -->
    <div class="social-media">
      <a href="https://www.facebook.com/profile.php?id=61583067824148" target="_blank" class="icon-link" aria-label="Facebook">
        <i class="fab fa-facebook"></i>
      </a>
      <a href="https://x.com/PhysicsP32590" target="_blank" class="icon-link" aria-label="Twitter">
        <i class="fab fa-twitter"></i>
      </a>
      <a href="https://www.instagram.com/physicsinphysics/" target="_blank" class="icon-link" aria-label="Instagram">
        <i class="fab fa-instagram"></i>
      </a>
      <a href="https://www.linkedin.com/me?trk=p_mwlite_feed-secondary_nav" target="_blank" class="icon-link" aria-label="LinkedIn">
        <i class="fab fa-linkedin"></i>
      </a>
      <a href="https://m.youtube.com/channel/UCQeEKqqidI7I0etj-Io0CZA/videos" target="_blank" class="icon-link" aria-label="YouTube">
        <i class="fab fa-youtube"></i>
      </a>
    </div>

    <!-- COPYRIGHT (SEPARATE, CANNOT AFFECT ICONS) -->
    <div class="copyright">
      © 2025 Vishal Shaw — CC BY-NC-ND 4.0. Partial reuse with credit only.
    </div>

  </div>

  <style>

    footer {
      width: 100%;
      display: flex;
      justify-content: center;
      padding: 1.5rem 0 1.2rem;
    }

    /* MAIN WRAPPER */
    .footer-inner {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 1.1rem; /* space between icons and copyright */
      width: 100%;
    }

    /* ICON ZONE LOCKED */
    .social-media {
      display: grid;
      grid-template-columns: repeat(5, minmax(55px, 75px));
      justify-content: center;
      gap: 20px;
      width: fit-content;
      max-width: 100%;
    }

    .icon-link {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 75px;
      height: 75px;
      font-size: 38px;
      color: white;
      border-radius: 16px;
      text-decoration: none;
      background: rgba(255,255,255,0.04);
      border: 1px solid rgba(59,130,246,0.3);
      box-shadow: 0 0 15px rgba(59,130,246,0.25);
      transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

    .icon-link:hover {
      transform: scale(1.12);
      box-shadow: 0 0 25px rgba(147,51,234,0.6);
    }

    .icon-link i {
      pointer-events: none;
    }

    /* COPYRIGHT */
    .copyright {
      font-size: 0.82rem;
      color: #aaa;
      text-align: center;
      max-width: 90%;
      line-height: 1.5;
    }

    /* MOBILE FIXES */
    @media (max-width: 768px) {
      .social-media {
        grid-template-columns: repeat(5, 60px);
        gap: 14px;
      }

      .icon-link {
        width: 60px;
        height: 60px;
        font-size: 28px;
      }
    }

    @media (max-width: 480px) {
      .social-media {
        grid-template-columns: repeat(3, 55px);
        row-gap: 10px;
      }

      .icon-link {
        width: 55px;
        height: 55px;
        font-size: 24px;
      }
    }
/* ✅ FORCE-LOCK ICONS ON MOBILE */
@media (max-width: 768px) {

  footer {
    padding-bottom: 0.8rem;
  }

  .footer-inner {
    gap: 0.8rem;
  }

  /* lock icon row size */
  .social-media {
    width: 100%;
    max-width: 360px;
    margin: 0 auto;
    justify-content: center;
  }

  /* force consistent sizing */
  .icon-link {
    min-width: 56px;
    max-width: 56px;
    min-height: 56px;
    max-height: 56px;
    font-size: 26px;
    padding: 0;
  }

  /* prevent wrapping glitch */
  .social-media a {
    flex-shrink: 0;
  }

  /* keep copyright out of icon space */
  .copyright {
    margin-top: 6px;
    padding-top: 4px;
  }
}
  </style>
</footer>